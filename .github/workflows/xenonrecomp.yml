# Assuming the workflow file content includes existing steps up to file validation.
name: Xenon Recomp Build

offer:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Validate Files
        run: |
          # Your file validation commands here

      # Debugging After Validation
      - name: Debug Workspace Files
        run: |
          find . -type f
      - name: Print Environment Variables
        run: |
          echo "XEX_FILE=${{ env.XEX_FILE }}"
          echo "CONFIG_FILE=${{ env.CONFIG_FILE }}"
          echo "PPC_CONTEXT_HEADER=${{ env.PPC_CONTEXT_HEADER }}"
